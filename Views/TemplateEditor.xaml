<Window x:Class="CmdRunnerPro.Views.TemplateEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Template Editor"
        Height="468" Width="1030"
        WindowStartupLocation="CenterOwner"
        UseLayoutRounding="True" SnapsToDevicePixels="True">
    <Grid Margin="10,10,10,10">
        <Grid.ColumnDefinitions>
            <!-- Fixed-width list column -->
            <ColumnDefinition Width="260"/>
            <!-- Flexible editor column -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Left: Templates List -->
        <GroupBox Header="Templates" Grid.Column="0" Grid.Row="0" Margin="10,0,0,0">
            <DockPanel Margin="10,0,-2,0">
                <ListBox x:Name="List" DisplayMemberPath="Name" SelectionChanged="List_SelectionChanged" Width="131"/>
                <StackPanel DockPanel.Dock="Bottom" Margin="0,6,0,0" Height="206" Width="110" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Add" Margin="5" Click="Add_Click" Height="30" Width="70"/>
                    <Button Content="Edit" Margin="5" Click="Edit_Click" Width="70" Height="30"/>
                    <Button Content="Delete" Margin="5" Click="Delete_Click" Width="70" Height="30"/>
                    <Button Content="Up" Margin="5" Click="Up_Click" Width="70" Height="30"/>
                    <Button Content="Down" Click="Down_Click" RenderTransformOrigin="2.383,0.289" Height="30" Width="70" Margin="5,5,5,5"/>
                </StackPanel>
            </DockPanel>
        </GroupBox>

        <!-- Right: Editor -->
        <GroupBox Header="Editor" Grid.Column="1" Grid.Row="0" Margin="50,0,10,0">
            <!-- Scroll so nothing is cut off at high DPI -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" CanContentScroll="False">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Name -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Margin="0,2,8,2" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="NameBox" MinWidth="320" MaxWidth="600"/>

                    <!-- Template -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Template:" Margin="0,8,8,2" VerticalAlignment="Top"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="TemplateBox"
                             AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                             Height="220" MinWidth="320" MaxWidth="600" FontFamily="Consolas"/>

                    <!-- Token palette -->
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Tokens:" Margin="0,8,8,2" VerticalAlignment="Center"/>
                    <WrapPanel Grid.Row="2" Grid.Column="1" Margin="0,8,0,2">
                        <Button Content="{}{comport1}" Click="InsertToken_Click" Tag="{}{comport1}" Margin="2"/>
                        <Button Content="{}{comport2}" Click="InsertToken_Click" Tag="{}{comport2}" Margin="2"/>
                        <Button Content="{}{username}"  Click="InsertToken_Click" Tag="{}{username}"  Margin="2"/>
                        <Button Content="{}{password}"  Click="InsertToken_Click" Tag="{}{password}"  Margin="2"/>
                        <Button Content="{}{opco}"      Click="InsertToken_Click" Tag="{}{opco}"      Margin="2"/>
                        <Button Content="{}{program}"   Click="InsertToken_Click" Tag="{}{program}"   Margin="2"/>
                        <Button Content="{}{wd}"        Click="InsertToken_Click" Tag="{}{wd}"        Margin="2"/>
                        <Button Content="{}{Q:program}" Click="InsertToken_Click" Tag="{}{Q:program}" Margin="2"/>
                    </WrapPanel>

                    <!-- Preview -->
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Preview:" Margin="0,8,8,2" VerticalAlignment="Top"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" x:Name="PreviewText" FontFamily="Consolas"
                               TextWrapping="Wrap" MinWidth="320" MaxWidth="600"/>

                    <!-- Actions -->
                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Preview" Click="Preview_Click" Margin="5"/>
                        <Button Content="Save Changes" Click="Save_Click" Margin="5"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </GroupBox>

        <!-- Bottom row: Close button -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Close" Width="100" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>